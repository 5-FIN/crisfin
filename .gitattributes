# 셸 스크립트·Gradle 래퍼는 항상 LF로 저장/체크아웃.
# (Windows에서 CRLF로 체크아웃되면 리눅스 빌드 컨테이너에서 shebang이 깨져
#  "./gradlew: not found"로 docker build가 실패함)
gradlew    text eol=lf
*.sh       text eol=lf

# Windows 배치 스크립트는 CRLF 유지.
*.bat      text eol=crlf
*.cmd      text eol=crlf
